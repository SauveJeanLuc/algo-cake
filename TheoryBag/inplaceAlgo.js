/*
In-Place Algorithms are those that does the operations
without creating a new copy of the input.
The changes are visible to other functions.
Thus being called destructive since it modifies the input during 
the function call.
*/

